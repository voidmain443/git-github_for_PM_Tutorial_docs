import re

path = '/Users/voidmain443/Agent_base_codebase/Project_M_crash_course/06_강사가이드/00_개설전_체크리스트.md'
with open(path, 'r') as f:
    content = f.read()

content = content.replace(
    '**조건부 개설 가능.** 차단 조건 3건을 닫으면 첫 기수를 열 수 있다.',
    '**개설 가능.** 차단 조건 3건이 닫혀 첫 기수를 열 수 있다.'
)

content = content.replace(
    '| 90 | **차단 조건 3건 착수** (§12) | — | 담당자와 기한이 정해졌다 | 이 문서 §12 |',
    '| 90 | ~~차단 조건 3건 착수 (§12)~~ | — | **전건 완료 (2026-09-14)** | 이 문서 §12 |'
)

content = content.replace(
    '## 12. ★ 개설 차단 조건 3건 — 이것을 닫기 전에는 첫 기수를 열지 않는다',
    '## 12. ~~★ 개설 차단 조건 3건 — 이것을 닫기 전에는 첫 기수를 열지 않는다~~ (완료)'
)

content = content.replace(
    '**차단 조건 합계 1.0 인-일.** T−30일 인쇄 발주 전에 끝나야 한다. **③은 인쇄 발주와 동시에 끝나지 않으면 재인쇄 비용이 발생한다.**',
    '**차단 조건 합계 1.0 인-일.** 2026-09-14 기준 전건 완료.'
)

content = content.replace(
    '| **①** | **`PD-25 · AI 기능 인수 기준 합의서 (A3)` 가 정규 캔버스 세트에 없다**',
    '| **①** | ~~**`PD-25 · AI 기능 인수 기준 합의서 (A3)` 가 정규 캔버스 세트에 없다**~~ (완료)'
)
content = content.replace(
    '| **②** | **PD-09 뒷면 최소 표본 조회표 4표가 실제로 인쇄되지 않으면',
    '| **②** | ~~**PD-09 뒷면 최소 표본 조회표 4표가 실제로 인쇄되지 않으면~~'
)
content = content.replace(
    '| **③** | **캔버스 인쇄면의 고유명사 치환 5건이 미반영이다**',
    '| **③** | ~~**캔버스 인쇄면의 고유명사 치환 5건이 미반영이다**~~'
)

with open(path, 'w') as f:
    f.write(content)
